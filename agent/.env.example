# Pulse RAG agent — copy to pulse/.env and fill in. NEVER commit the real .env.
LLM_API_KEY=sk-your-aicredits-key-here
LLM_BASE_URL=https://api.aicredits.in/v1
PULSE_LLM_MODEL=deepseek/deepseek-v4-flash

# Optional: lock the chat UI down to your demo origin(s), comma-separated.
CORS_ORIGINS=*
